#include<stdio.h>
int main()
{
    arr =[1,2,0,5,6,0,8,9]
    temp[]
    for(i=0;i<n;i++)
    {
        if(arr[i]==0)
        {
            temp[j] = arr[i]
            j++;
        }
        else
        {continue;
        }
    }
}